#  Thinking with Images综述

# 1. 介绍

- **演变**：视觉模态充当**静态**的语境 → 真正能用图像辅助思考（**主动查询、修改甚至生成新视觉信息**）
- **原则**：将视觉表征作为一种可操作、可验证的思考形式，从而让模型在认知过程中，能以视觉信息为中间步骤，**主动进行观察、操作与推理**
- 三阶段发展：
  - 充当 “指挥官” 协调外部视觉工具
  -  “视觉程序员” 生成代码以执行定制化操作
  - 具备内在想象与模拟能力的 “视觉思考者”

<img src="/Users/fengnian/Desktop/study/work/think with image/assets/image-20251206181527625.png" alt="image-20251206181527625" style="zoom: 50%;" />  

（放大图片；添加辅助线；**分步操作流程**）

# 2. 三个范式

<img src="/Users/fengnian/Desktop/study/work/think with image/assets/image-20251208141645593.png" alt="image-20251208141645593" style="zoom:50%;" /> 

以一项复杂任务为例：**规划如何重新布置杂乱的房间，来容纳一张新的大型沙发。**

### 2.1 第一阶段：工具驱动视觉探索

模型作为 “**规划者**” ➡️ 协调一组固定的**外部视觉模块**（为当前子任务选择合适的工具）

> 针对家具摆放问题，模型可能会先调用 “**目标检测器**（object_detector）” 识别房间内物品，再调用 “**距离估算器**（distance_estimator）” 测量可用空间。由于模型的推理基于这些工具的输出，它可能会得出结论：“当前空隙为 1.5 米，而新沙发需要 2.0 米的空间，因此无法容纳”。
>
> 这种方法在针对性数据采集方面效果显著，但受限于预定义工具集的**静态功能**，存在一定局限性。

#### 具体过程：

- LMM的操作对象是一套预设的**外部视觉工具集**，记为\(T_{tool}=\{T_1,...,T_m\}\)。每个工具\(T_i\)都具备特定的、封装好的功能。模型的核心任务是根据对问题的实时理解，**判断应使用哪款工具及如何应用**。在推理的每一步t，核心动作是 LMM 的策略生成一个符号化的工具调用指令\(c_t\)—— 该指令会明确指定从工具集中选中的工具（\(T_{sel}\in T\)）以及执行工具所需的必要参数\(a_{tool}\)。指令生成的概率公式如下：

  \(c_t \sim P(\cdot|S_t, I, Q;\Theta_{LMM})\)，\(c_t = (T_{sel}, a_{tool})\)（即指令包含选中的工具与参数）。

- 生成的指令随后会传递至外部工具执行引擎\(\mathcal{E}_{tool}\)，该引擎会对图像\(I\)执行指定操作，并返回输出结果\(o_t\)：   \[o_{t}=\mathcal{E}_{tool }\left(c_{t}, I\right) \quad 其中\ o_t \in \mathcal{T}_{text} \cup \mathcal{I}_{vis} \quad \] 

#### **工具分类**：

| 工具类别         | 核心作用                                     | 典型案例                                                |
| ---------------- | -------------------------------------------- | ------------------------------------------------------- |
| 语义接地工具     | 连接像素与语言，将视觉转化为模型可理解的文本 | OCR 提取图像文字、生成任务相关图像描述（如 PromptCap）  |
| 感知探索工具     | 突破单帧静态感知，主动挖掘视觉细节           | 图像缩放（ZoomEye）、目标检测、语义分割（DetToolChain） |
| 视觉推理辅助工具 | 将抽象推理外化为视觉操作，辅助逻辑梳理       | 画几何辅助线、标记计数目标、屏蔽无关区域（VAT）         |

#### 实现方法：

- 依托上下文学习的**提示驱动**方法：

  在**无需更新参数**的情况下，协调预设的外部视觉工具。精心设计的提示会将静态视觉输入转化为可主动探索的工作空间，并依托模型的**上下文学习**，实现稳健的问题求解。

  - **以语言为媒介的协作**：由视觉 - 语言模型提供感知见解，再交由大型语言模型处理，纯文本模型无需修改架构，就能作为视觉推理系统发挥作用

  - **通过输入操作优化感知**：通过简单的输入修改和系统性探索，提升LMMs的感知能力。

    - **视觉提示工程**：在图像上绘制红圈，引导模型关注特定区域，提升零样本性能。

    - **思维可视化**：通过提示模型生成 ASCII 网格等文本化视觉表征，进一步增强空间推理能力。

      > 举个例子：如果要表示一张 “中间有个方块、右上角有个小圆” 的简单图像，ASCII 网格可能长这样
      >
      > ```plaintext
      > · · · · ·
      > · · · · O
      > · · # · ·
      > · · · · ·
      > · · · · ·
      > ```

    - **视觉抽象思维**：将视觉输入简化为草图或轮廓等抽象形式，在过滤干扰噪声的同时保留关键结构信息。

    系统性探索方面，引入无训练的**树状搜索**策略，使模型能够执行类人缩放操作并探索图像层级；利用模型内部注意力，自动裁剪并放大至相关区域。

  - **整合专业视觉专家**：让通用LMM使用专业视觉专家模型

    - **SoM prompting（标记集提示）**：依赖**图像分割模型**，先让分割模型划分图像区域，再给区域叠加符号标记；

      > 例如：让 GPT-4V 等模型通过引用标记，精准定位图像中的具体区域，解决 “视觉接地不准” 问题（避免大模型描述物体时 “指东说西”）

    - **DetToolChain**：使用各类视觉处理工具（如目标检测、分类模型），将 “视觉处理工具” 与 “结构化**检测推理链**” 结合，分解复杂任务，降低大模型直接处理的误差

    - **MCTS（蒙特卡洛树搜索）**：使用能反馈 **重点区域**的视觉专家（如边缘检测、显著性检测模型）；实现 LMMs 与专家的双向交互 —— 大模型初步聚焦区域，专家反馈调整建议，反复优化注意力；

    - **Visual Thoughts（视觉思维框架）**：将专家模型的输出（如分割结果、检测结果）称为 “中间视觉思维”，作为信息**缓存**；避免大模型反复调用专家

- 通过示例传授流程执行能力的有**监督微调**方法：教会大型多模态模型（LMMs）使用外部工具或内部视觉技能。在 **展示工具使用流程步骤的数据集**上对模型进行微调

  - **学习协调工具**：
    - 通过多模态指令数据进行微调，管理 **技能库**，能根据用户意图激活合适的工具。
    - 在大规模合成的 **思维 - 动作链（CoTA）**轨迹数据集 上训练，可学会生成**推理步骤**、用正确语法调用 OCR或计算器等外部工具，还能整合工具返回的信息。
    - 借助 SFT 监督模型生成 “多步视觉推理轨迹”，进而在整个推理过程中，实现 “动态且由验证器引导的视觉信息使用”

  

- 实现工具使用策略优化的**强化学习**方法。

### 2.2 第二阶段：程序化视觉操作

模型作为 “**视觉程序员**” ➡️ 生成**可执行代码**来完成定制化的**视觉分析**

> 针对家具摆放问题，模型可借助 matplotlib 这类绘图库生成 **Python 脚本** —— 该脚本会创建房间的 **2D 俯视平面图**，将每件家具以图形形式呈现。随后，模型能通过编程在这个抽象视觉空间中测试多种新摆放方案。这种方法兼具灵活性，且生成的代码可作为模型思维过程的透明、可验证记录。但该阶段的主要局限仍在于，模型运行代码需**依赖外部执行环境**。

### 2.3 第三阶段：内在视觉想象

模型通过**内在想象**实现了认知自主性。它借助统一的生成架构**生成新图像作为中间推理步骤**，从而摆脱了对外部执行的依赖。

> 为找到最优房间布局，模型可直接生成一张新的照片级真实感图像 —— 画面中呈现的是家具重新摆放后、新沙发已就位的房间场景。这张内部生成的图像充当了 “视觉假设” 或 “心智模拟” 的角色：模型随后能将该图像反馈给自身，对新布局进行评估，比如发现某条通道现在被挡住了。这一过程在闭环认知循环中实现了无缝且真正一体化的视觉思维形式，解决了前两个阶段存在的架构瓶颈。

### 2.4 特点

- **非线性**：这三个阶段并非代表严格的线性递进关系 ➡️ 根据实际需要使用不同策略（混合使用？）
- 核心特征：将视觉信息视为推理过程本身中的**可操作**中间步骤

### 2.5 挑战

视觉信息具有**连续且密集**的特性（而这一特性正是其优势的来源），但同时也造成了涉及**效率、稳健性与泛化能力**的根本性障碍。

- **计算成本，视觉思维的令牌爆炸效应**：

  视觉信息处理的成本高出数个数量级。单张图像会被分解为由**数千个视觉 patches**构成的密集网格，每个 patch 都需要**独立**的高强度计算；即便生成一个中间视觉步骤，也属于高成本的计算操作。而将这些操作串联起来的多步推理过程，会形成叠加的倍增式负担。

- **信息密度：一张有缺陷的图像 vs 千句错误文字**：

  视觉信息的**密集特性**导致错误以独特的**破坏性方式传播**。然而，视觉错误会破坏对现实的底层感知。初始错误为所有后续思考确立了**虚假的基准事实**，进而造成感知层面的根本性错误 —— 其相互关联的谬误将破坏整个推理过程。

  > 试想某模型使用放大工具识别某产品的材质：若它**错误地聚焦**于产品下方的木质桌子，其后续分析不会简单地失败，反而可能**围绕完全无关的主题**展开一条新的、内部自洽的推理链 —— 比如生成一段关于木纹和清漆光泽的、听起来合理的分析。

- **架构割裂：语言与像素间的瓶颈**：

  当前 “以图思考”（Thinking with Images）模型采用**分离视觉与语言系统**的模块化设计。这种分离给迭代式视觉思考带来了挑战：架构必须将丰富的空间信息转化为序列格式，而这一过程存在**丢失细粒度细节**的风险。

  理想状态下，真正的视觉思考需要一个动态循环 —— 让模型能在形成**意图与感知即时结果之间交替**进行。因此，构建能让推理引擎与视觉画布（visual canvas）实现更直接交互的架构，成为该领域的关键发展方向。

- **跨任务泛化：单一视觉策略并非万能**

  单一的视觉思考模式无法适配视觉任务的多样性 ，研发能够 “掌握多样化视觉策略库”，并能学习 “元策略”（meta-policy）为特定任务选择适配策略的**智能体**，仍是该领域一项复杂且尚未解决的挑战。